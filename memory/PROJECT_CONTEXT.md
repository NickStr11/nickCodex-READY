# PROJECT_CONTEXT

## Проект

- Название: `nickCodex-READY`
- Тип: переносимый agent context pack
- Цель: дать Никите рабочую агентную среду без долгой инициализации

## Что уже есть

- root contract через `AGENTS.md`
- раздельные слои `rules/`, `skills/`, `knowledge/`
- входящий слой `inbox/`
- личный профиль в `aboutme.md` и глубокие контекстные файлы рядом

## Текущий фокус

- превратить пакет из просто профиля в рабочую лошадку
- держать корень чистым и не складывать туда сырые артефакты
- добавить минимальную живую память проекта без раздувания контекста
- запустить ежедневный цикл через skills и `inbox/`

## Ограничения

- это не приложение и не монорепа с тяжёлой инфраструктурой
- один источник истины лучше нескольких совместимых, но разъехавшихся файлов
- активный контекст должен оставаться маленьким

## Следующие шаги

- pressure-test `daily-session`, `handoff-session` и `capture-to-knowledge` на реальных задачах
- развивать `skills/` под повторяемые workflows
- при необходимости добавить лёгкий operational-слой без превращения пакета в `cortex`
- держать `runtime/` отдельным от инструкций и долгоживущего контекста
