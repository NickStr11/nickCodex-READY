# Runtime Imports

Сюда можно временно складывать сырые выгрузки, куски чатов, логи и другой
материал до разборки.

После разбора:

- полезное переносится в `memory/`, `persona/`, `handoff/` или `knowledge/`
- мусор удаляется
