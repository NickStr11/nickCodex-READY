# Legacy Import

Сюда складывается curated import из `{{LEGACY_SOURCE_NAME}}`.

Правило:

- не копировать всё подряд
- переносить только полезную long-term память
- у каждого импорта указывать, откуда он взят и когда разобран
