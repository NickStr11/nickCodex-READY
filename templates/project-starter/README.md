# {{PROJECT_NAME}}

Project-local workspace scaffold.

Start here:
- `AGENTS.md`
- `.codex/config.toml`
- `memory/PROJECT_CONTEXT.md`
- `inbox/now.md`
- `memory/diary/` for finished-session history

Validate project context with:

```powershell
.\scripts\validate-project-context.ps1
```

Smoke-check Codex subagents with:

```powershell
.\scripts\smoke-codex-subagents.ps1
```
